# 📊 Руководство по HTML отчетам

## 🚀 Генерация HTML отчетов

### Базовый запуск с отчетом

```bash
# Запуск всех тестов с генерацией HTML отчета
python3 run_tests.py --report

# Или напрямую через pytest
python3 -m pytest --html=reports/report.html --self-contained-html
```

### Параметры отчета

- `--html=reports/report.html` - путь к файлу отчета
- `--self-contained-html` - создает автономный HTML файл (все стили и скрипты встроены)

## 📋 Что содержит HTML отчет

### 1. Общая информация
- **Время выполнения** - общее время всех тестов
- **Количество тестов** - общее количество, успешных, неудачных, пропущенных
- **Метаданные** - версия Python, платформа, установленные пакеты

### 2. Детальная информация по тестам
- **Название теста** - полный путь к тесту
- **Статус** - PASSED/FAILED/SKIPPED
- **Время выполнения** - время выполнения каждого теста
- **Ошибки** - полный стек ошибок для неудачных тестов